# SignSpeak

A Vite + React front end for SignSpeak, an accessibility project for real-time sign-language communication.

## Development

```bash
npm install
npm run dev
```

Create a production build with `npm run build` and preview it with `npm run preview`.

The current contact, feedback, and account flows intentionally persist demo data locally and download JSON snapshots. No backend is connected yet.
